# v0.2.0 — Real Anthropic smoke output

**Date**: 2026-04-25
**Command**: `drift handoff --session 3d132809-e586-435c-a4c2-4060b093dcc2 --to claude-code --print`
**Model**: `claude-opus-4-7` (default)
**Source**: 1 claude-code session, 6 turns (the shortest session in `/tmp/drift-smoke-v011/.prompts/events.db`)
**Wall time**: ≈ 6 seconds end-to-end

## Notable behaviour

The session in question was a `/login` + `/exit` pair with no actual coding work.
**Opus correctly identified the empty-task case** and produced a "cold start" brief
asking the next agent to ask the user for goal/branch/files. This is the kind of
handoff-aware reasoning a Haiku-only default would have struggled with.

## Output (verbatim)

```markdown
# Handoff Brief — `(no branch)`

| Field      | Value |
|------------|-------|
| From       | claude-code ×1 (1 sessions, 6 turns) |
| To         | claude-code |
| Generated  | 2026-04-25 07:00 UTC |
| Branch dif | +0 / -0 across 0 files |

## What I'm working on

There is no active coding task to hand off. The only source session in scope (3d132809, claude-code) recorded a login with an immediate exit and no development activity. No branch, files, decisions, or errors were captured. A receiving agent should treat this as a cold start and solicit task intent from the user before proceeding.

## Progress so far

- ⏸ No task has been initiated in the prior session

## Files in scope

_(no file changes)_

## Key decisions made

_(none recorded)_

## Approaches tried but rejected

_(none)_

## Open questions / blockers

1. What task or branch should the next session actually work on?

## Next steps (suggested)

1. Ask the user to state the goal, target branch, and any relevant files before writing code.

---

*Generated by `drift handoff` against (local repo) on 2026-04-25 07:00 UTC.*

## How to continue (paste this to claude-code)

> I'm picking up an in-progress task documented in the handoff brief above. Read it end-to-end, then resume from "Next steps #1". The codebase is at the current working directory. Don't revisit decisions in "Key decisions made"; treat them as settled.
```

## Multi-session scope (next smoke, run later in dev cycle)

A more representative `drift handoff --branch <name> --to claude-code` against a
real branch with mid-flight work will be re-run during Phase 3 demo recording.
The shape of the output is identical; the LLM-summary "What I'm working on"
becomes substantive.

## Cost

The Opus call for this smoke was within token budget caps:

- Input: ≈ 1.4k tokens (template + session metadata + 1 file summary)
- Output: ≈ 0.4k tokens (JSON envelope)
- Cost: ≈ \$0.05 USD (Opus at \$15/\$75 per Mtok)

For Phase 3's demo recording, expected cost per `drift handoff` invocation is
\$0.10–\$0.30 depending on branch size — within the v0.2 dev-cycle ceiling of \$1.00.
